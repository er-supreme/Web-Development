console.log('this is sachin');

setTimeout(() => {
    
    for (let index = 0; index < 200; index++) {
        const element = index;
        console.log(element);
        
    }
}, 2000);

console.log('done');qw